{
  "name": "strtok3",
  "version": "3.0.0",
  "description": "A promise based streaming tokenizer",
  "author": {
    "name": "Borewit",
    "url": "https://github.com/Borewit"
  },
  "scripts": {
    "clean": "del-cli lib/** src/**/*.js src/**/*.js.map src/**/*.d.ts test/**/*.js test/**/*.js.map",
    "compile-src": "tsc -p src",
    "compile-test": "tsc -p test",
    "compile": "npm run compile-src && npm run compile-test",
    "build": "npm run clean && npm run compile",
    "lint-ts": "tslint 'src/**/*.ts' --exclude 'src/**/*.d.ts' 'test/**/*.ts' --exclude 'test/**/*.d.ts'",
    "lint-md": "remark -u preset-lint-recommended .",
    "lint": "npm run lint-md && npm run lint-ts",
    "lint-fix": "tslint --fix 'src/**/*.ts' 'test/**/*.ts'",
    "test": "mocha --require ts-node/register --require source-map-support/register --full-trace test",
    "test-coverage": "nyc npm run test",
    "coveralls": "npm run test-coverage && nyc report --reporter=text-lcov | coveralls",
    "start": "npm run compile && npm run lint && npm run cover-test"
  },
  "engines": {
    "node": ">=6"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Borewit/strtok3"
  },
  "license": "MIT",
  "main": "./lib/index.js",
  "typings": "lib/index",
  "bugs": {
    "url": "https://github.com/Borewit/strtok3/issues"
  },
  "devDependencies": {
    "@types/chai": "^4.1.7",
    "@types/mocha": "^5.2.7",
    "@types/node": "^12.0.4",
    "chai": "^4.2.0",
    "coveralls": "^3.0.3",
    "del-cli": "^2.0.0",
    "mocha": "^6.1.4",
    "nyc": "^14.1.1",
    "remark-cli": "^7.0.0",
    "remark-preset-lint-recommended": "^3.0.2",
    "source-map-support": "^0.5.12",
    "ts-node": "^8.3.0",
    "tslint": "^5.11.0",
    "typescript": "^3.5.1"
  },
  "dependencies": {
    "debug": "^4.1.1",
    "then-read-stream": "^2.0.0",
    "token-types": "^1.0.1"
  },
  "keywords": [
    "tokenizer",
    "promise",
    "parser",
    "binary",
    "endian",
    "integer",
    "uint",
    "token",
    "read",
    "stream",
    "streaming"
  ],
  "nyc": {
    "check-coverage": false,
    "extension": [
      ".ts"
    ],
    "sourceMap": true,
    "instrument": true,
    "reporter": [
      "lcov",
      "text"
    ],
    "report-dir": "coverage"
  }
}
